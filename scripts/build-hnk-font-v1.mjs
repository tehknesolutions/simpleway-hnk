import fs from'node:fs';import crypto from'node:crypto';const cmap=JSON.parse(fs.readFileSync(new URL('../font/HNK_FONT_CMAP_V1.json',import.meta.url),'utf8'));const cps=cmap.mappings.map(x=>parseInt(x.codepoint.slice(2),16));if(cps.length!==92||new Set(cps).size!==92)throw new Error('CMAP_NOT_BIJECTIVE_92');for(let i=0;i<40;i++)if(cps[i]!==0xE100+i)throw new Error('ROOT_RANGE_MISMATCH');for(let i=0;i<52;i++)if(cps[40+i]!==0xE200+i)throw new Error('MX1_RANGE_MISMATCH');const payload=JSON.stringify({family:cmap.fontFamily,version:cmap.fontVersion,mappings:cmap.mappings});console.log(JSON.stringify({status:'FONT_SOURCE_CONTRACT_READY',glyphs:92,cmapSha256:crypto.createHash('sha256').update(payload).digest('hex'),formatsRequested:['OTF','TTF','WOFF2'],note:'Binary font compilation requires a font compiler/build environment; this script validates deterministic source contract only.'},null,2));