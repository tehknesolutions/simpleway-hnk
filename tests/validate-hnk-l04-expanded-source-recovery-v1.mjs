import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/recovery/l04-expanded-source-recovery.v1.json',import.meta.url),'utf8'));assert.equal(a.results.historicalBindingIdentitiesRecovered,0);assert.equal(a.results.historicalHnkSurfacesRecovered,0);assert.equal(a.results.existingCountEvidence.languageBindings,9);assert.equal(a.results.existingCountEvidence.recoveredBindings,9);assert.equal(a.newAuthoringGuard.mustUseNewVersion,true);assert.equal(a.newAuthoringGuard.slots,9);console.log('PASS L04 EXPANDED SOURCE RECOVERY: GitHub authorities exhausted, V2 authoring eligible');