import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
async function json(path){return JSON.parse(await readFile(new URL(`../${path}`,import.meta.url),'utf8'));}
const gate=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-activation-72-authoring-human-batch.v1.json');
const snap=await json('progress/CYCLE1_PROGRESS_SNAPSHOT_V67.json');
assert.equal(gate.status,'AWAITING_EXPLICIT_HUMAN_APPROVAL');
assert.equal(gate.pack_decisions.length,6);
assert.equal(gate.governance_decisions.length,6);
assert.equal(gate.checks.architecture_approved,72);
assert.equal(gate.checks.packs,6);
assert.equal(gate.checks.slots_per_pack,12);
assert.equal(gate.checks.activation_authored_before_gate,0);
assert.equal(gate.checks.activation_validated_before_gate,0);
assert.equal(gate.checks.new_surface_forms,0);
assert.equal(gate.checks.new_grammar_rules,0);
assert.equal(gate.checks.authority_promotions,0);
assert.equal(snap.L02.activation_authored,0);
assert.equal(snap.L02_activation_authoring.status,'AWAITING_EXPLICIT_HUMAN_APPROVAL');
assert.equal(snap.next_gate,'SWHNK-L02-ACTIVATION-72-AUTHORING-HUMAN-BATCH-V1');
console.log('PASS SWHNK-L02-ACTIVATION-72-AUTHORING-GATE-PENDING-V1');
