import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-WRITING-GAP-RESOLUTION-BATCH-V1.json',import.meta.url),'utf8'));assert.equal(a.inputGapCount,17);assert.equal(a.summary.resolved+a.summary.unresolved,17);for(const r of a.rows.filter(x=>x.resolution==='RESOLVED_GID_SEQUENCE'))assert.ok(r.gIds.length>0);console.log('PASS HNK-WRITING-GAP-RESOLUTION-V1',a.summary);