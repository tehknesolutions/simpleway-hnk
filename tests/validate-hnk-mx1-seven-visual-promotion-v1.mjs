import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../proposals/language/HNK_MX1_SEVEN_VISUAL_PROMOTION_APPROVED_TRANSITION_V1.json',import.meta.url),'utf8'));assert.equal(a.status,'ACTIVE');assert.equal(a.approval.humanDecision,'APPROVED_7_OF_7');assert.equal(a.promoted.length,7);assert.equal(a.result.previousDerivedMx1Count,52);assert.equal(a.result.newDerivedMx1Count,59);assert.equal(new Set(a.promoted.map(x=>x.mx1Key)).size,7);for(const x of a.promoted)assert.equal(x.visualStatus,'CANONICAL_DERIVED_MX1');console.log('PASS HNK-MX1-SEVEN-VISUAL-PROMOTION-V1 52 -> 59');