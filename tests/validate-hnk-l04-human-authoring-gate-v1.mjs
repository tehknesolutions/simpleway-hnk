import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/authoring/l04-human-authoring-gate.v1.json',import.meta.url),'utf8'));assert.equal(a.decisionSlots.length,9);assert.equal(a.curriculumTarget.hnkSurface,null);assert.equal(a.sourceState.recoveredPhrases,0);assert.ok(a.decisionSlots.every(x=>x.proposedHnkSurface===null));assert.ok(a.decisionSlots.every(x=>x.proposalStatus==='NEW_AUTHORED_CANDIDATE_AWAITING_VALIDATION'));assert.equal(a.humanGate.required,true);console.log('PASS L04 HUMAN AUTHORING GATE: framework only, 0 invented surfaces');