import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/authoring/l04-v2-nine-slot-authoring-batch.v1.json',import.meta.url),'utf8'));assert.equal(a.slots.length,9);assert.equal(a.summary.newLexemesProposed,0);assert.equal(a.summary.hnkSurfacesProposed,0);assert.ok(a.slots.every(x=>x.proposedHnkSurface===null));assert.equal(a.curriculumTarget.hnkSurface,null);assert.ok(a.compositionCandidates.forbiddenExamples.includes('ERU VALI KE'));console.log('PASS L04 V2 NINE-SLOT AUTHORING BATCH: 9 slots, 0 invented surfaces');