import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/authoring/l04-v2-dayi-intention-microgrammar-proposal.v1.json',import.meta.url),'utf8'));assert.equal(a.options.length,4);assert.equal(a.recommendation.selectedForHumanReview,'A');assert.deepEqual(a.options[0].examples,['ERU DAYI KE']);assert.equal(a.options[0].classification,'NEW_AUTHORED_CANDIDATE_AWAITING_VALIDATION');assert.ok(a.forbiddenGeneralizations.includes('DAYI does not automatically take objects.'));console.log('PASS L04 DAYI INTENTION PROPOSAL: human decision required');