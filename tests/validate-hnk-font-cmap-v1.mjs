import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const c=JSON.parse(await readFile(new URL('../font/HNK_FONT_CMAP_V1.json',import.meta.url),'utf8'));assert.equal(c.count,92);assert.equal(c.mappings.length,92);assert.equal(new Set(c.mappings.map(x=>x.codepoint)).size,92);assert.equal(c.mappings[0].codepoint,'U+E100');assert.equal(c.mappings[39].codepoint,'U+E127');assert.equal(c.mappings[40].codepoint,'U+E200');assert.equal(c.mappings[91].codepoint,'U+E233');assert.equal(c.legacyHenuvokodan12TokenPuaExcluded,true);console.log('PASS HNK-FONT-CMAP-V1 92/92');