import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
async function json(path){return JSON.parse(await readFile(new URL(`../${path}`,import.meta.url),'utf8'));}
const gate=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-teacher-notes-validation-human-batch.v1.json');
const transition=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-teacher-notes-validation.applied-transition.v1.json');
const artifact=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-teacher-notes-001-003.validated.v1.json');
const snap=await json('progress/CYCLE1_PROGRESS_SNAPSHOT_V66.json');
assert.equal(gate.status,'APPROVED_ALL_DECISIONS');
assert.equal(gate.note_decisions.length,3);
assert.equal(gate.governance_decisions.length,5);
assert.equal(transition.status,'APPLIED');
assert.deepEqual(transition.after.GLOBAL,{MISSING:808,AUTHORED:0,VALIDATED:200,FROZEN:0});
assert.deepEqual(transition.after.L02,{MISSING:94,AUTHORED:0,VALIDATED:45,FROZEN:0});
assert.equal(artifact.status,'VALIDATED_SCOPED_COURSE_USE');
assert.equal(artifact.notes.length,3);
assert.equal(artifact.checks.validated,3);
assert.equal(artifact.checks.new_surface_forms_created,0);
assert.equal(artifact.checks.new_grammar_rules_created,0);
assert.equal(artifact.checks.authority_promotions,0);
assert.equal(snap.L02.teacher_notes_validated,3);
assert.equal(snap.next_gate,'SWHNK-L02-ACTIVATION-72-SOURCE-REVIEW-HUMAN-BATCH-V1');
console.log('PASS SWHNK-L02-TEACHER-NOTES-VALIDATED-V1');
