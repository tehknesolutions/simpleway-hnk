import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-A1-READING-WRITING-ATLAS-V1_1.json',import.meta.url),'utf8'));assert.equal(a.summary.entries,20);assert.equal(a.summary.glossSupported,20);assert.equal(a.summary.missingGloss,0);for(const id of ['L01-OPI-002','L01-OPI-007A','L01-OPI-007B','L01-OPI-009']){const e=a.entries.find(x=>x.id===id);assert.ok(e.meaningEvidence);assert.notEqual(e.meaning,'SOURCE_DOES_NOT_SUPPLY_GLOSS_HERE')}console.log('PASS HNK-A1-GLOSS-RECOVERY 4/4; ATLAS 20/20');