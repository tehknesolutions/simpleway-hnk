import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/checkpoints/l04-v2-consolidated-checkpoint.v1.json',import.meta.url),'utf8'));assert.equal(a.validatedMicrogrammars.length,5);assert.equal(a.metrics.validatedExactSurfaces,8);assert.equal(a.metrics.fullyWritableSurfaces,8);assert.equal(a.metrics.runtimeActiveStructures,0);assert.equal(a.releaseAssessment.runtime,'HOLD');assert.equal(a.sourceManifestState.runtimePromotionAllowed,false);assert.ok(a.stillBlocked.includes('SARU = usually.'));console.log('PASS L04 V2 CONSOLIDATED CHECKPOINT: 5 microgrammars, 8 surfaces, runtime HOLD');