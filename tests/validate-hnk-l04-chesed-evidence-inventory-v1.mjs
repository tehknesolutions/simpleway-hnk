import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-L04-CHESED-EVIDENCE-INVENTORY-V1.json',import.meta.url),'utf8'));assert.equal(a.status,'HOLD_INSUFFICIENT_EVIDENCE');assert.equal(a.inventory.languageBindingsDeclared,9);assert.equal(a.inventory.recoveredPhrases,0);assert.equal(a.inventory.authorityDistribution.WATCH,1);assert.equal(a.inventory.authorityDistribution.GATE,8);assert.equal(a.writingCoverage.auditableSurfaces,0);assert.equal(a.curriculumTarget.hnkTranslation,'UNAUTHORIZED_BY_INFERENCE');console.log('PASS L04 CHESED EVIDENCE INVENTORY: HOLD');