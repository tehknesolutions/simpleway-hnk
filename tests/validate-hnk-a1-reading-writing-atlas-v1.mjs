import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-A1-READING-WRITING-ATLAS-V1.json',import.meta.url),'utf8'));assert.equal(a.summary.entries,20);assert.equal(a.summary.fullyWritable,20);const q=a.entries.find(x=>x.id==='CANON-HNK-QUESTION');assert.equal(q.transliteration,'EN ZAMI HNK KE');assert.equal(q.meaning,'Você fala HNK?');assert.equal(q.segmentation.find(x=>x.form==='HNK').status,'CONTENT_TOKEN_PRESERVED');for(const e of a.entries)assert.ok(e.hnkWriting.length>0);console.log('PASS HNK-A1-READING-WRITING-ATLAS-V1 20/20');