import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
async function json(path){return JSON.parse(await readFile(new URL(`../${path}`,import.meta.url),'utf8'));}
const gate=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-teacher-notes-authoring-human-batch.v1.json');
const transition=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-teacher-notes-authoring.applied-transition.v1.json');
const artifact=await json('curriculum/cycle-01/L02-chokhmah/authoring/teacher-notes.v1.json');
const validationGate=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-teacher-notes-validation-human-batch.v1.json');
const snap=await json('progress/CYCLE1_PROGRESS_SNAPSHOT_V65.json');
assert.equal(gate.status,'APPROVED_ALL_DECISIONS');
assert.equal(gate.note_decisions.length,3);
assert.equal(gate.governance_decisions.length,5);
assert.equal(transition.status,'APPLIED');
assert.equal(artifact.status,'AUTHORED_3_OF_3_VALIDATION_PENDING');
assert.equal(artifact.notes.length,3);
assert.equal(artifact.metrics.AUTHORED,3);
assert.equal(artifact.metrics.VALIDATED,0);
assert.equal(artifact.metrics.new_HNK_lexical_forms,0);
assert.equal(artifact.metrics.new_HNK_grammar_rules,0);
assert.equal(artifact.metrics.authority_promotions,0);
assert.equal(validationGate.status,'AWAITING_EXPLICIT_HUMAN_APPROVAL');
assert.equal(snap.L02.teacher_notes_authored,3);
assert.equal(snap.L02.teacher_notes_validated,0);
assert.equal(snap.next_gate,'SWHNK-L02-TEACHER-NOTES-VALIDATION-HUMAN-BATCH-V1');
console.log('PASS SWHNK-L02-TEACHER-NOTES-AUTHORED-3-VALIDATION-PENDING-V1');
