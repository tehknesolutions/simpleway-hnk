import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-A1-SENTENCE-WRITING-CORPUS-V2.json',import.meta.url),'utf8'));assert.equal(a.summary.utterances,20);assert.equal(a.summary.fullyWritable,20);assert.equal(a.summary.partial,0);assert.deepEqual(a.summary.unresolvedTokenTypes,[]);const q=a.utterances.find(x=>x.id==='CANON-HNK-QUESTION');assert.equal(q.hnk,'EN ZAMI HNK KE');assert.equal(q.tokens.find(x=>x.form==='HNK').status,'CONTENT_TOKEN_PRESERVED');for(const u of a.utterances)assert.equal(u.coverage.unresolved.length,0);console.log('PASS HNK-A1-SENTENCE-WRITING-CORPUS-V2 20/20');