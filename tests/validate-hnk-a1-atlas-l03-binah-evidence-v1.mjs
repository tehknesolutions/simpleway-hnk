import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-A1-ATLAS-L03-BINAH-EVIDENCE-V1.json',import.meta.url),'utf8'));assert.equal(a.validatedVocabulary.length,8);assert.equal(a.blockedVocabularySlots.length,8);assert.equal(a.validatedStructures.length,3);const opi=a.validatedStructures.find(x=>x.id==='L03-OPI-POS1');assert.equal(opi.translationContract,'UNRESOLVED_NOT_ASSERTED');assert.equal(a.validatedVocabulary.find(x=>x.transliteration==='SARU').gloss_pt,'todos os dias / habitualidade');console.log('PASS L03 BINAH ATLAS EVIDENCE V1 8 validated / 8 blocked');