import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-A1-SENTENCE-WRITING-CORPUS-V1.json',import.meta.url),'utf8'));const q=a.utterances.find(x=>x.id==='CANON-HNK-QUESTION');assert.equal(q.hnk,'EN ZAMI HNK KE');assert.equal(q.tokens.find(x=>x.form==='HNK').status,'CONTENT_TOKEN_PRESERVED');assert.ok(a.summary.partial>0);assert.ok(a.summary.unresolvedTokenTypes.length>0);for(const u of a.utterances)for(const t of u.tokens.filter(x=>x.status==='NOT_IN_A1_WRITING_ATLAS'))assert.equal(t.writing,null);console.log('PASS HNK-A1-SENTENCE-WRITING-CORPUS-V1',a.summary);