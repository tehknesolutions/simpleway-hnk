import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/validation/l04-v2-sali-option-a-linguistic-validation.v1.json',import.meta.url),'utf8'));assert.equal(a.status,'VALIDATED_L04_ONLY_NON_RUNTIME');assert.equal(a.validatedSurface.surface,'ERU SALI KE');assert.equal(a.microgrammar.productivity,'EXACT_SURFACE_ONLY');assert.equal(a.authorityPreservation.SALI,'WATCH');assert.equal(a.microgrammar.objectComplementLicensed,false);assert.equal(a.microgrammar.vemiComplementLicensed,false);assert.equal(a.runtimeActive,false);console.log('PASS L04 SALI OPTION A VALIDATION: exact surface, WATCH preserved, non-runtime');