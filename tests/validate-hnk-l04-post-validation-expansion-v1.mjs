import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/authoring/l04-v2-post-validation-expansion.v1.json',import.meta.url),'utf8'));assert.equal(a.counts.total,9);assert.equal(a.counts.admissibleWithoutNewGrammar,2);assert.equal(a.counts.requiresNewMicrogrammar,5);assert.equal(a.counts.blockedByAuthorityOrRole,2);assert.equal(a.counts.newAutomaticallyAdmittedFillers,0);assert.deepEqual(a.validatedMicrogrammar.allowedActivityFillers,['VALI','PARAZAMI']);console.log('PASS L04 POST-VALIDATION EXPANSION: 2/5/2, no automatic expansion');