import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import{renderCanonicalMx1Svg}from'../src/hnk/mx1-canonical-visual-renderer-v2.mjs';const m=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-MX1-VISUAL-MANIFEST-V2.json',import.meta.url),'utf8'));assert.equal(m.items.length,52);let rendered=0;for(const x of m.items){const svg=renderCanonicalMx1Svg(x);assert.match(svg,/HNK40 VISUAL-CANON-V2 roots/);for(const g of x.gIds)assert.ok(svg.includes('#'+g));rendered++;}assert.equal(rendered,52);console.log('PASS MX1-REAL-VISUAL-GATE-V2 52/52');