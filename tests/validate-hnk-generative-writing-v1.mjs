import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import{encodeWriting}from'../src/hnk/generative-writing-v1.mjs';const c=JSON.parse(await readFile(new URL('../font/HNK_FONT_CMAP_V1.json',import.meta.url),'utf8'));const known=c.mappings.find(x=>x.kind==='MX1');const a=encodeWriting([known.gIds],c)[0];assert.equal(a.status,'CANONICAL_DERIVED_MX1');assert.equal(a.codepoint,known.codepoint);const b=encodeWriting([['G01','G02']],c)[0];assert.equal(b.status,'UNRESOLVED_GID_SEQUENCE');const d=encodeWriting([['G21','G01']],c)[0];assert.ok(['CANONICAL_DERIVED_MX1','GENERATED_CANDIDATE'].includes(d.status));console.log('PASS HNK-GENERATIVE-WRITING-V1');
