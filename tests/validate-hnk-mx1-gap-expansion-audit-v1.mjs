import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-MX1-GAP-EXPANSION-AUDIT-V1.json',import.meta.url),'utf8'));assert.equal(a.summary.words,17);assert.equal(a.summary.syllabificationUnresolved,0);assert.equal(a.summary.governedWords,17);assert.equal(new Set(a.newMx1Candidates).size,a.newMx1Candidates.length);for(const r of a.rows)assert.equal(r.syllabification,'GOVERNED_V1');console.log('PASS HNK-MX1-GAP-EXPANSION-AUDIT-V1',a.summary);