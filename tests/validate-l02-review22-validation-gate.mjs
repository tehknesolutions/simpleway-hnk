import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
async function json(path){return JSON.parse(await readFile(new URL(`../${path}`,import.meta.url),'utf8'));}
const gate=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-review-22-validation-human-batch.v1.json');
const lane=await json('curriculum/cycle-01/L02-chokhmah/authoring/review-lane.v1.json');
const validated=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-review-001-022.validated.v1.json');
const transition=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-review-22-validation.applied-transition.v1.json');
const snap=await json('progress/CYCLE1_PROGRESS_SNAPSHOT_V72.json');
assert.equal(gate.status,'APPROVED_ALL_DECISIONS');
assert.equal(gate.group_decisions.filter(x=>x.decision==='APPROVED').length,6);
assert.equal(gate.governance_decisions.filter(x=>x.decision==='APPROVED').length,6);
assert.equal(gate.checks.authored_review,22);
assert.equal(gate.checks.validated_before_gate,0);
assert.equal(gate.checks.OPI006_block_preserved,true);
assert.equal(lane.metrics.AUTHORED,22);
assert.equal(lane.metrics.VALIDATED,0);
assert.equal(validated.status,'VALIDATED_22_OF_22_SCOPED_COURSE_USE');
assert.equal(validated.validated_slots.length,22);
assert.equal(new Set(validated.validated_slots.map(x=>x.slot_id)).size,22);
assert.ok(validated.validated_slots.every(x=>x.validation_state==='VALIDATED'));
assert.equal(validated.metrics.new_hnk_forms_created,0);
assert.equal(validated.metrics.new_HNK_grammar_rules,0);
assert.equal(validated.metrics.language_authority_promotions,0);
assert.equal(transition.status,'APPLIED_VALIDATION_ONLY_NO_CLOSURE');
assert.deepEqual(transition.after.L02,{MISSING:0,AUTHORED:0,VALIDATED:139,FROZEN:0});
assert.deepEqual(transition.after.GLOBAL,{MISSING:714,AUTHORED:0,VALIDATED:294,FROZEN:0});
assert.equal(transition.effects.L02_sealed,false);
assert.equal(transition.effects.L03_authoring_started,false);
assert.equal(snap.L02_review_validation.status,'VALIDATED_22_OF_22_SCOPED_COURSE_USE');
assert.equal(snap.next_gate,'SWHNK-L02-CHOKHMAH-CLOSURE-HUMAN-BATCH-V1');
console.log('PASS SWHNK-L02-REVIEW-22-VALIDATED-SCOPED-COURSE-USE-V1');
