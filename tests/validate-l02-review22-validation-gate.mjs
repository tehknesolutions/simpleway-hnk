import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
async function json(path){return JSON.parse(await readFile(new URL(`../${path}`,import.meta.url),'utf8'));}
const gate=await json('curriculum/cycle-01/L02-chokhmah/validation/l02-review-22-validation-human-batch.v1.json');
const lane=await json('curriculum/cycle-01/L02-chokhmah/authoring/review-lane.v1.json');
const snap=await json('progress/CYCLE1_PROGRESS_SNAPSHOT_V71.json');
assert.equal(gate.status,'AWAITING_EXPLICIT_HUMAN_APPROVAL');
assert.equal(gate.group_decisions.length,6);
assert.equal(gate.governance_decisions.length,6);
assert.equal(gate.checks.authored_review,22);
assert.equal(gate.checks.validated_before_gate,0);
assert.equal(gate.checks.groups,6);
assert.equal(gate.checks.new_surface_forms,0);
assert.equal(gate.checks.new_grammar_rules,0);
assert.equal(gate.checks.authority_promotions,0);
assert.equal(gate.checks.OPI006_block_preserved,true);
assert.equal(lane.metrics.AUTHORED,22);
assert.equal(lane.metrics.VALIDATED,0);
assert.equal(snap.L02_review_validation.status,'AWAITING_EXPLICIT_HUMAN_APPROVAL');
assert.equal(snap.next_gate,'SWHNK-L02-REVIEW-22-VALIDATION-HUMAN-BATCH-V1');
console.log('PASS SWHNK-L02-REVIEW-22-VALIDATION-GATE-PENDING-V1');
