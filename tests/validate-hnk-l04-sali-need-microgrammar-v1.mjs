import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/authoring/l04-v2-sali-need-microgrammar-proposal.v1.json',import.meta.url),'utf8'));assert.equal(a.options.length,4);assert.equal(a.recommendation.selectedForHumanReview,'A');assert.deepEqual(a.options[0].examples,['ERU SALI KE']);assert.equal(a.lexicalEvidence.authority,'WATCH');assert.ok(a.forbiddenGeneralizations.includes('SALI does not automatically license VEMI.'));console.log('PASS L04 SALI NEED PROPOSAL: human decision required');