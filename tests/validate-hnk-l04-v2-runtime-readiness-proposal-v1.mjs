import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/runtime/l04-v2-runtime-readiness-proposal.v1.json',import.meta.url),'utf8'));assert.equal(a.runtimeAllowlist.flatMap(x=>x.surfaces).length,8);assert.equal(a.requiredPrePromotionGates.length,8);assert.equal(a.promotionStateMachine.current,'RUNTIME_HOLD');assert.equal(a.promotionStateMachine.afterProposalApproval,'READINESS_CONTRACT_APPROVED_RUNTIME_STILL_HOLD');assert.equal(a.architecture.legacyLayer.mutationPolicy,'DO_NOT_REWRITE_AS_RECOVERED');assert.equal(a.humanGate.approvalEffect.includes('Runtime remains HOLD.'),true);console.log('PASS L04 V2 RUNTIME READINESS PROPOSAL: 8 surfaces, runtime HOLD');