import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-L03-BINAH-WRITING-COVERAGE-V1.json',import.meta.url),'utf8'));assert.equal(a.summary.lexicalForms,9);assert.equal(a.summary.controlledSurfaces,6);assert.equal(new Set(a.missingMx1Candidates).size,a.missingMx1Candidates.length);for(const r of a.rows)assert.equal(r.syllabification,'GOVERNED_V1');console.log('PASS L03 BINAH WRITING COVERAGE',a.summary);