import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/authoring/l04-v2-saru-habitual-scope-proposal.v1.json',import.meta.url),'utf8'));assert.equal(a.options.length,4);assert.equal(a.recommendation.selectedForHumanReview,'A');assert.deepEqual(a.options[0].examples,['SARU ERU VALI KE','SARU ERU PARAZAMI KE']);assert.equal(a.lexicalEvidence.authority,'WATCH');assert.ok(a.forbiddenGeneralizations.includes('SARU is not usually.'));assert.ok(a.forbiddenGeneralizations.includes('TAMURI position does not prove SARU position.'));console.log('PASS L04 SARU HABITUAL SCOPE PROPOSAL: human decision required');