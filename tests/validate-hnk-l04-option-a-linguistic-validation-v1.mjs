import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/validation/l04-v2-option-a-linguistic-validation.v1.json',import.meta.url),'utf8'));assert.equal(a.status,'VALIDATED_L04_ONLY_NON_RUNTIME');assert.equal(a.validatedSurfaces.length,2);assert.equal(a.microgrammar.productivity,'CLOSED_ALLOWLIST_ONLY');assert.deepEqual(a.microgrammar.allowedActivityFillers,['VALI','PARAZAMI']);assert.equal(a.runtimeActive,false);assert.equal(a.authorityPreservation.universalGrammarPromoted,false);console.log('PASS L04 OPTION A VALIDATION: 2 scoped surfaces, non-runtime');