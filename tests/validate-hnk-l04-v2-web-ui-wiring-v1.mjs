import assert from 'node:assert/strict';import fs from 'node:fs';const s=fs.readFileSync(new URL('../experiments/a1-rc1-sprint1/web/app.mjs',import.meta.url),'utf8');assert.match(s,/routeAfterCampaignLevel/);assert.match(s,/renderL04V2Bridge/);assert.match(s,/completeL04V2BridgeMission/);assert.match(s,/Entrar na Ponte Chesed/);assert.match(s,/L17_NE_TRAP/);assert.match(s,/5\)<100/);console.log('PASS L04 V2 WEB UI WIRING STATIC CONTRACT: L16 → bridge UI → L17 route present; 5-mission progress; 32-level counter preserved');