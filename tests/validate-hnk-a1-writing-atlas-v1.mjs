import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../experiments/a1-rc1-sprint1/HNK-A1-WRITING-ATLAS-V1.json',import.meta.url),'utf8'));assert.equal(a.summary.registryEntries,43);assert.equal(a.summary.renderableWords,42);assert.equal(a.summary.excludedContentTokens,1);assert.equal(a.summary.missingCodepoints,0);assert.equal(a.words.find(x=>x.form==='HNK').status,'CONTENT_TOKEN_EXCLUDED');for(const w of a.words.filter(x=>x.status==='CANONICAL_A1_WRITING_SEQUENCE'))assert.ok(w.codepoints.every(Boolean));console.log('PASS HNK-A1-WRITING-ATLAS-V1 42/42 + HNK excluded');