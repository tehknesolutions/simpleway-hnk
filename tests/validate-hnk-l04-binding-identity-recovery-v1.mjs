import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/recovery/l04-binding-identity-recovery.v1.json',import.meta.url),'utf8'));assert.equal(a.findings.cycleManifest.languageBindings,9);assert.equal(a.findings.cycleManifest.recoveredBindings,9);assert.equal(a.findings.repositoryArchaeology.individualBindingNamesRecovered,0);assert.equal(a.slotResolution.length,9);assert.ok(a.slotResolution.every(x=>x.bindingIdentity==='UNAVAILABLE_IN_CURRENT_REPOSITORY'&&x.hnkSurface===null));console.log('PASS L04 BINDING IDENTITY RECOVERY: 9/9 count proven, 0 identities materialized');