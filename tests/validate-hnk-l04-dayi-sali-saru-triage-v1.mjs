import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/authoring/l04-v2-dayi-sali-saru-semantic-triage.v1.json',import.meta.url),'utf8'));assert.equal(a.items.length,3);assert.equal(a.triageSummary.automaticAdmissions,0);assert.equal(a.triageSummary.separateMicrogrammarsRequired,2);assert.equal(a.triageSummary.separateTemporalScopeGatesRequired,1);assert.equal(a.recommendedSequence[0].id,'SWHNK-L04-DAYI-INTENTION-MICROGRAMMAR-PROPOSAL-V1');assert.ok(a.nonInference.includes('SARU is not usually.'));console.log('PASS L04 DAYI SALI SARU TRIAGE: 0 automatic admissions');