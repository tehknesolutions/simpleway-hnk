import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/authoring/l04-v2-third-person-direct-question-microgrammar-proposal.v1.json',import.meta.url),'utf8'));assert.equal(a.options.length,4);assert.equal(a.recommendation.selectedForHumanReview,'A');assert.equal(a.options[0].classification,'NEW_AUTHORED_CANDIDATE_AWAITING_VALIDATION');assert.equal(a.evidence.ERU.genderDistinctionLicensed,false);assert.equal(a.evidence.KE.automaticProductivity,false);assert.ok(a.forbiddenGeneralizations.includes('No English auxiliary does morpheme is created.'));console.log('PASS L04 MICROGRAMMAR PROPOSAL: human decision required');