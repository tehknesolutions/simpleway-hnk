import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const a=JSON.parse(await readFile(new URL('../curriculum/cycle-01/L04-chesed/validation/l04-v2-saru-option-a-linguistic-validation.v1.json',import.meta.url),'utf8'));assert.equal(a.status,'VALIDATED_L04_ONLY_NON_RUNTIME');assert.equal(a.validatedSurfaces.length,2);assert.equal(a.microgrammar.productivity,'CLOSED_ALLOWLIST_ONLY');assert.equal(a.authorityPreservation.SARU,'WATCH');assert.equal(a.microgrammar.tamuriTransfer,false);assert.equal(a.microgrammar.dayiCompatibilityLicensed,false);assert.equal(a.microgrammar.saliCompatibilityLicensed,false);assert.equal(a.runtimeActive,false);assert.ok(a.explicitNonInference.includes('SARU_NOT_USUALLY'));console.log('PASS L04 SARU OPTION A VALIDATION: 2 scoped surfaces, WATCH preserved, non-runtime');