import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const g=JSON.parse(await readFile(new URL('../proposals/language/HNK_MX1_SEVEN_CANDIDATE_HUMAN_VISUAL_GATE_V1.json',import.meta.url),'utf8'));assert.equal(g.candidateCount,7);assert.equal(g.items.length,7);assert.equal(new Set(g.items.map(x=>x.mx1Key)).size,7);for(const x of g.items){assert.equal(x.visualStatus,'GENERATED_CANDIDATE_AWAITING_HUMAN_VISUAL_GATE');assert.ok(x.gIds.length>=1&&x.gIds.length<=3)}console.log('PASS HNK-MX1-SEVEN-VISUAL-GATE-V1 7/7');